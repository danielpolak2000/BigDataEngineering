apiVersion: v1
kind: Service
metadata:
  name: dashboard
  labels:
    app: crypto-tracker
    component: dashboard
spec:
  type: {{ .Values.dashboard.service.type }}
  ports:
  - port: {{ .Values.dashboard.service.port }}
    targetPort: 5000
  selector:
    app: crypto-tracker
    component: dashboard
